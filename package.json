{
	"name": "cli-portfolio",
	"version": "1.0.0",
	"description": "A terminal-based portfolio, that can be called anywhere with NPM.",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc && npm run mv-config",
		"dev": "tsc && npm run mv-config && node dist/index.js",
		"mv-config": "ncp ./src/config ./dist/config"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^20.11.30",
		"ncp": "^2.0.0",
		"typescript": "^5.4.3"
	},
	"dependencies": {
		"boxen": "^7.1.1",
		"chalk": "^5.3.0",
		"clear": "^0.1.0",
		"inquirer": "^9.2.16",
		"open": "^10.1.0"
	}
}
